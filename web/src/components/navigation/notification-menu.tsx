import { Bell, CheckCheck, Info } from "lucide-react";
import { useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { Button } from "@/components/ui/button";
export function NotificationMenu() { const [open, setOpen] = useState(false); return <div className="relative"><Button variant="ghost" size="icon" onClick={() => setOpen(!open)} aria-label="Open notifications" aria-expanded={open} aria-haspopup="menu" className="relative"><Bell className="h-4 w-4" /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-primary" /></Button><AnimatePresence>{open && <motion.div initial={{ opacity: 0, y: 6, scale: .98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 6, scale: .98 }} transition={{ duration: .16 }} role="menu" className="absolute right-0 top-12 z-50 w-72 rounded-xl border bg-card p-1.5 shadow-elevated"><div className="flex items-center justify-between border-b px-3 py-2.5"><p className="text-xs font-semibold">Notifications</p><button className="focus-ring rounded text-[10px] text-muted-foreground hover:text-foreground"><CheckCheck className="mr-1 inline h-3 w-3" /> Mark read</button></div><div className="flex gap-3 rounded-lg p-3"><Info className="mt-0.5 h-4 w-4 shrink-0 text-primary" /><div><p className="text-xs font-medium">Your notification center is ready</p><p className="mt-1 text-[11px] leading-5 text-muted-foreground">Processing updates and workspace alerts will appear here.</p></div></div></motion.div>}</AnimatePresence></div>; }
