import { BookOpen, CircleHelp, LogOut, Settings2, UserRound } from "lucide-react";
import { useState } from "react";
import { Link } from "react-router-dom";
import { AnimatePresence, motion } from "framer-motion";
import { Button } from "@/components/ui/button";
export function UserMenu() { const [open, setOpen] = useState(false); return <div className="relative"><Button variant="ghost" size="sm" onClick={() => setOpen(!open)} aria-expanded={open} aria-haspopup="menu" className="gap-2 px-2"><span className="grid h-7 w-7 place-items-center rounded-full bg-secondary text-xs font-semibold text-secondary-foreground">AR</span><span className="hidden text-xs font-medium lg:inline">Alex Rivera</span><Settings2 className="hidden h-3.5 w-3.5 text-muted-foreground lg:inline" /></Button><AnimatePresence>{open && <motion.div initial={{ opacity: 0, y: 6, scale: .98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 6, scale: .98 }} transition={{ duration: .16 }} role="menu" className="absolute right-0 top-12 z-50 w-56 rounded-xl border bg-card p-1.5 shadow-elevated"><div className="border-b px-3 py-2.5"><p className="text-xs font-semibold">Alex Rivera</p><p className="mt-0.5 text-[11px] text-muted-foreground">Workspace owner</p></div><MenuLink icon={UserRound} label="Profile" href="/settings" onClick={() => setOpen(false)} /><MenuLink icon={Settings2} label="Preferences" href="/settings" onClick={() => setOpen(false)} /><MenuLink icon={BookOpen} label="Documentation" href="#documentation" onClick={() => setOpen(false)} /><MenuLink icon={CircleHelp} label="Help center" href="#help" onClick={() => setOpen(false)} /><div className="my-1 border-t" /><button role="menuitem" onClick={() => setOpen(false)} className="focus-ring flex w-full items-center gap-2 rounded-md px-3 py-2 text-xs text-destructive hover:bg-destructive/10"><LogOut className="h-3.5 w-3.5" />Log out</button></motion.div>}</AnimatePresence></div>; }
function MenuLink({ icon: Icon, label, href, onClick }: { icon: typeof UserRound; label: string; href: string; onClick: () => void }) { return <Link role="menuitem" to={href} onClick={onClick} className="focus-ring flex items-center gap-2 rounded-md px-3 py-2 text-xs text-muted-foreground hover:bg-muted hover:text-foreground"><Icon className="h-3.5 w-3.5" />{label}</Link>; }
