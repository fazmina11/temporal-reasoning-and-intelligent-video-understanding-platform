import { AnimatePresence, motion } from "framer-motion";
import { Command, FileText, Search, UploadCloud, X } from "lucide-react";
import { Link } from "react-router-dom";
import { Button } from "@/components/ui/button";
export function CommandPalette({ open, onClose }: { open: boolean; onClose: () => void }) { return <AnimatePresence>{open && <><motion.button initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={onClose} className="fixed inset-0 z-[60] bg-slate-950/40 backdrop-blur-sm" aria-label="Close command palette" /><motion.div initial={{ opacity: 0, y: -12, scale: .98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: -12, scale: .98 }} transition={{ duration: .18 }} role="dialog" aria-modal="true" aria-label="Command palette" className="fixed left-1/2 top-[12vh] z-[61] w-[min(38rem,calc(100%-2rem))] -translate-x-1/2 overflow-hidden rounded-2xl border bg-card shadow-elevated"><div className="flex items-center gap-3 border-b px-4"><Search className="h-4 w-4 text-muted-foreground" /><input autoFocus placeholder="Search pages and actions…" className="focus-ring h-14 flex-1 bg-transparent text-sm outline-none placeholder:text-muted-foreground" aria-label="Search pages and actions" /><kbd className="hidden rounded border bg-muted px-1.5 py-0.5 font-mono text-[10px] text-muted-foreground sm:block">ESC</kbd><Button variant="ghost" size="icon" onClick={onClose} aria-label="Close command palette"><X className="h-4 w-4" /></Button></div><div className="p-2"><p className="px-3 py-2 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Quick actions</p><CommandLink icon={UploadCloud} label="Upload video" href="/videos/new" onClick={onClose} /><CommandLink icon={FileText} label="Open video library" href="/videos" onClick={onClose} /><CommandLink icon={Command} label="Keyboard shortcuts" href="#shortcuts" onClick={onClose} /></div><div className="border-t px-4 py-2 text-[10px] text-muted-foreground">Command palette placeholder · functionality will be added in a later phase.</div></motion.div></>}</AnimatePresence>; }
function CommandLink({ icon: Icon, label, href, onClick }: { icon: typeof Command; label: string; href: string; onClick: () => void }) { return <Link to={href} onClick={onClick} className="focus-ring flex items-center gap-3 rounded-lg px-3 py-3 text-sm hover:bg-muted"><span className="grid h-7 w-7 place-items-center rounded-md bg-secondary text-secondary-foreground"><Icon className="h-3.5 w-3.5" /></span>{label}</Link>; }
