import { Bell, ChevronDown, Command, Menu, Moon, Search, Sun } from "lucide-react";
import { Link } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { Breadcrumbs } from "@/components/navigation/breadcrumbs";
import { useTheme } from "@/providers/theme-provider";
import { UserMenu } from "@/components/navigation/user-menu";
import { NotificationMenu } from "@/components/navigation/notification-menu";
export function TopNavigation({ onOpenMobile, onOpenCommand }: { onOpenMobile: () => void; onOpenCommand: () => void }) { const { theme, toggleTheme } = useTheme(); return <header className="sticky top-0 z-30 border-b bg-background/80 backdrop-blur-xl"><div className="flex h-16 items-center gap-3 px-4 lg:px-7"><Button variant="ghost" size="icon" className="lg:hidden" onClick={onOpenMobile} aria-label="Open navigation"><Menu className="h-4 w-4" /></Button><div className="hidden min-w-0 flex-1 md:block"><Breadcrumbs /></div><button type="button" onClick={onOpenCommand} className="focus-ring hidden h-9 w-full max-w-xs items-center gap-2 rounded-md border bg-background px-3 text-left text-xs text-muted-foreground transition hover:bg-muted md:flex" aria-label="Open command palette"><Search className="h-3.5 w-3.5" /><span>Search anything</span><span className="ml-auto flex items-center gap-0.5 rounded border bg-muted px-1.5 py-0.5 font-mono text-[10px]"><Command className="h-2.5 w-2.5" />K</span></button><div className="ml-auto flex items-center gap-1"><Button variant="ghost" size="icon" className="md:hidden" onClick={onOpenCommand} aria-label="Open search"><Search className="h-4 w-4" /></Button><Button variant="ghost" size="icon" onClick={toggleTheme} aria-label={`Switch to ${theme === "dark" ? "light" : "dark"} theme`}>{theme === "dark" ? <Sun className="h-4 w-4" /> : <Moon className="h-4 w-4" />}</Button><NotificationMenu /><Button size="sm" className="ml-1 hidden sm:inline-flex" asChild><Link to="/videos/new">Upload Video <span className="hidden xl:inline">↗</span></Link></Button><UserMenu /></div></div><div className="border-t px-4 py-2 md:hidden"><Breadcrumbs /></div></header>; }
