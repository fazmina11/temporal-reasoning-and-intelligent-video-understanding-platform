import { useEffect, useState } from "react";
import { Outlet, useLocation } from "react-router-dom";
import { CommandPalette } from "@/components/navigation/command-palette";
import { Sidebar } from "@/components/navigation/sidebar";
import { TopNavigation } from "@/components/navigation/top-navigation";
import { FloatingActionButton } from "@/components/global/floating-action-button";
export function DashboardShell() { const [collapsed, setCollapsed] = useState(false); const [mobileOpen, setMobileOpen] = useState(false); const [commandOpen, setCommandOpen] = useState(false); const location = useLocation(); useEffect(() => { setMobileOpen(false); }, [location.pathname]); useEffect(() => { const onKeyDown = (event: KeyboardEvent) => { if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "k") { event.preventDefault(); setCommandOpen(true); } if (event.key === "Escape") { setCommandOpen(false); setMobileOpen(false); } }; window.addEventListener("keydown", onKeyDown); return () => window.removeEventListener("keydown", onKeyDown); }, []); return <div className="min-h-screen bg-background"><a href="#main-content" className="focus-ring sr-only fixed left-3 top-3 z-[100] rounded-md bg-card px-3 py-2 text-sm shadow-elevated focus:not-sr-only">Skip to content</a><Sidebar collapsed={collapsed} mobileOpen={mobileOpen} onToggle={() => setCollapsed((value) => !value)} onCloseMobile={() => setMobileOpen(false)} /><div className={collapsed ? "lg:pl-[76px]" : "lg:pl-64"}><TopNavigation onOpenMobile={() => setMobileOpen(true)} onOpenCommand={() => setCommandOpen(true)} /><main id="main-content" className="min-h-[calc(100vh-4rem)] px-4 py-6 pb-24 sm:px-6 lg:px-8"><Outlet /></main></div><FloatingActionButton /><CommandPalette open={commandOpen} onClose={() => setCommandOpen(false)} /></div>; }
